const state = {
}


// getters 用来获取属性的
const getters = {
}


// actions 对外提供的方法 
const actions = {
}

// mutations 必须同步执行 改变对象属性 必须在这里执行
const mutations = {
}

export default {
	namespaced: true,
	state: state,
	getters: getters,
	actions,
	mutations
}